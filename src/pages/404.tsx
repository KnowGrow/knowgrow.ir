import React from "react"

const NotFound = () => <React.Fragment>Page not found!</React.Fragment>

export default NotFound

const newFunction = (title, description)=> {
    const newTitle = "Hello" + title 
    return
        <React.Fragment>Page not found!</React.Fragment>
    
}